import wollok.game.*
import jugador.*
import mundo.*
import config.*

describe "tests de la isla enemigos" {
	method initialize() {
		stats.cambiarPersonaje(new Personaje(image="luffyQuieto.jpg", position = game.at(0, game.height() / 2), positionAnterior = null))
	}
	
	test "perderVida() saca vida a jugador" {
		stats.perderVida() // cambiar metodo sin wollok games
		const ultimo = stats.vida().last()
		assert.that(ultimo === cora4)
	}
}